# =============================================================================
# HavenWoodHollow — Environment Configuration
# =============================================================================
# Copy this file to .env and customise values for your setup.
# All scripts in scripts/ will read .env automatically.
# Command-line flags always override values set here.
# =============================================================================

# TCP port for the Unity MCP Bridge (default: 8090)
UNITY_MCP_PORT=8090

# Absolute path to your Unity project (used by install / verify / uninstall)
# UNITY_PROJECT=/Users/you/Projects/MyGame

# Path to the Node.js binary (auto-detected if not set)
# NODE_PATH=/usr/local/bin/node
